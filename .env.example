DATABASE_URL="postgresql://milhao_user:SUA_SENHA@postgres:5432/milhao_solidario?schema=public"
NODE_ENV="production"
MERCADO_PAGO_ACCESS_TOKEN="APP_USR-OU-TEST-SEU_TOKEN_AQUI"
APP_URL="https://seu-dominio.com.br"
NEXT_PUBLIC_APP_URL="https://seu-dominio.com.br"

# Admin real da Fase 4
ADMIN_EMAIL="admin@mural29.local"
ADMIN_PASSWORD="troque-essa-senha-forte"

# Fallback emergencial: se preencher, ainda permite /admin?secret=SUA_SENHA_ADMIN
ADMIN_API_SECRET=""

# Opcional: deixe false quando testar em domínio sem HTTPS ou proxy interno HTTP.
# Em HTTPS real, pode trocar para true depois.
ADMIN_COOKIE_SECURE="false"

# Opcional, mas recomendado em Docker/EasyPanel para upload persistente.
# Aponte para uma pasta com volume montado e permissão de escrita.
UPLOAD_DIR="/app/uploads"
